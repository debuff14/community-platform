# =====================================================================
# 本地敏感配置样例
# 使用方法：复制本文件为 application-local.yml，按本机环境修改后使用
# 注意：application-local.yml 已在 .gitignore 中忽略，不要提交到仓库
# =====================================================================
spring:
  datasource:
    username: root
    password: 你的数据库密码
  data:
    redis:
      host: localhost
      port: 6379
