# 云上家园 前端

Vue 3 + Vite + Element Plus + Pinia + Vue Router + Axios。

## 开发启动

```bash
npm install
npm run dev
```

开发服务器端口 `5173`，`/api` 已代理到后端 `http://localhost:8080`，先启动后端再访问前端。

## 构建

```bash
npm run build
```
